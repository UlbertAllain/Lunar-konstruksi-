import Link from "next/link";

import type { HydratedCmsSection, PublicPageContext } from "@/features/public-site/public-site.types";

import { PublicContactForm } from "../public-contact-form";
import { asRecord, joinLocation, pickImage, pickText, slugToHref } from "../public-helpers";
import { ArchiveFrame, ArchiveLabel, ArchivePlaceholder, ArchiveStamp, RuledTitle } from "./archive-ui";

const container="mx-auto w-full max-w-[1360px] px-4 sm:px-6 lg:px-8";

type PageKey="about"|"services"|"projects"|"contact";

function firstRecord(sections:HydratedCmsSection[]){for(const section of sections){if(section.data?.length)return asRecord(section.data[0]);}return {} as Record<string,unknown>}

function ArchiveHero({context,pageKey}:{context:PublicPageContext;pageKey:PageKey}){const hero=context.sections.find(s=>s.type==="hero");const record=firstRecord(context.sections);const image=pickImage(record,"");const title=typeof hero?.content.title==="string"&&hero.content.title.trim()?hero.content.title.trim():context.page?.title||pageKey.toUpperCase();const desc=typeof hero?.content.description==="string"&&hero.content.description.trim()?hero.content.description.trim():"Public company record generated from Lunar CMS.";return <section className="bg-[#E9DFD0] py-6"><div className={container}><ArchiveFrame><div className="grid lg:grid-cols-[0.9fr_1.1fr]"><div className="p-7 sm:p-10"><div className="flex gap-2"><ArchiveLabel>Page / {pageKey}</ArchiveLabel><ArchiveLabel>CMS / Published</ArchiveLabel></div><h1 className="mt-10 font-serif text-[58px] uppercase leading-[0.84] tracking-[-0.055em] sm:text-[82px]">{title}</h1><div className="mt-6 h-[2px] w-20 bg-[#C94A28]"/><p className="mt-6 max-w-xl text-[12px] leading-7 text-[#2C2925]/60">{desc}</p><div className="mt-10"><ArchiveStamp>Public<br/>Record</ArchiveStamp></div></div><div className="min-h-[380px] border-t border-[#2C2925]/18 bg-[#DCCFBD] lg:border-l lg:border-t-0">{image?<img src={image} alt={title} className="h-full min-h-[380px] w-full object-cover"/>:<ArchivePlaceholder label={title}/>}</div></div></ArchiveFrame></div></section>}

function DataSection({section,index}:{section:HydratedCmsSection;index:number}){const records=section.data.map(asRecord);const title=typeof section.content.title==="string"&&section.content.title.trim()?section.content.title.trim():section.type.charAt(0).toUpperCase()+section.type.slice(1);if(["hero","cta","intro","stats","process","gallery"].includes(section.type)) return null;
return <section className={`${index%2?"bg-[#E7DCCB]":"bg-[#F4EBDD]"} py-14 lg:py-18`}><div className={container}><RuledTitle index={String(index+1).padStart(2,"0")} label={`${section.type} / ${section.variant}`} title={title}/><div className="mt-7 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{records.map((record,i)=>{const name=pickText(record,["title","name","question","authorName","clientName","fullName"],`${section.type} ${i+1}`);const desc=pickText(record,["summary","excerpt","description","answer","quote","message","content","role","position"],"");const img=pickImage(record,"");const base=section.type==="projects"?"/projects":section.type==="services"?"/services":"";const href=base?slugToHref(base,record,base):"#";const body=<><div className="h-[220px] border-b border-[#2C2925]/18 bg-[#DCCFBD]">{img?<img src={img} alt={name} className="h-full w-full object-cover"/>:<ArchivePlaceholder label={name}/>}</div><div className="p-4"><p className="font-mono text-[8px] font-black uppercase tracking-[0.14em] text-[#C94A28]">REC / {String(i+1).padStart(2,"0")}</p><h3 className="mt-3 font-serif text-[26px] leading-[0.95]">{name}</h3>{desc?<p className="mt-3 text-[11px] leading-6 text-[#2C2925]/56">{desc}</p>:null}{section.type==="projects"?<p className="mt-3 font-mono text-[8px] uppercase tracking-[0.1em] text-[#2C2925]/42">{joinLocation(record)||"Location —"}</p>:null}</div></>;return href!=="#"?<Link href={href} key={`${name}-${i}`} className="border border-[#2C2925]/18 bg-[#F5EBDD] transition hover:-translate-y-1">{body}</Link>:<article key={`${name}-${i}`} className="border border-[#2C2925]/18 bg-[#F5EBDD]">{body}</article>})}</div></div></section>}

function Narrative({context}:{context:PublicPageContext}){const sections=context.sections.filter(s=>["intro","stats","process"].includes(s.type));if(!sections.length)return null;return <section className="bg-[#F4EBDD] py-14"><div className={container}><div className="grid gap-4 lg:grid-cols-3">{sections.map((section,i)=><div key={section.id} className={`min-h-[220px] border border-[#2C2925]/18 p-5 ${i===1?"bg-[#ECD896]":"bg-[#E8DDCD]"}`}><ArchiveLabel>{section.type} / {String(i+1).padStart(2,"0")}</ArchiveLabel><h3 className="mt-7 font-serif text-[30px] leading-[0.98]">{typeof section.content.title==="string"?section.content.title:section.type.toUpperCase()}</h3>{typeof section.content.description==="string"?<p className="mt-4 text-[11px] leading-6 text-[#2C2925]/58">{section.content.description}</p>:null}</div>)}</div></div></section>}

function ContactBlock({context}:{context:PublicPageContext}){return <section className="bg-[#E7DCCB] py-16"><div className={container}><div className="grid gap-8 lg:grid-cols-[0.36fr_0.64fr]"><div><RuledTitle index="99" label="Project Intake" title="Open New Record" copy="Masukkan kebutuhan proyek. Data akan masuk ke Leads CMS dan menjadi catatan tindak lanjut tim Lunar."/></div><div className="border border-[#2C2925]/18 bg-[#F4EBDD] p-5"><PublicContactForm settings={{whatsapp:context.settings.contact.whatsapp,email:context.settings.contact.email,phone:context.settings.contact.phone}}/></div></div></div></section>}

export function ArchivePageRenderer({context,pageKey}:{context:PublicPageContext;pageKey:PageKey}){return <div className="bg-[#E9DFD0] text-[#231F1B]"><ArchiveHero context={context} pageKey={pageKey}/><Narrative context={context}/>{context.sections.map((section,index)=><DataSection key={section.id} section={section} index={index}/>)}{pageKey==="contact"?<ContactBlock context={context}/>:null}</div>}
