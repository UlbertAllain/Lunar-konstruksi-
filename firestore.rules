rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Semua operasi CMS dilakukan melalui Firebase Admin SDK di server.
    // Client tidak diberikan akses langsung ke Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
